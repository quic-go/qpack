run:
linters-settings:
linters:
  disable-all: true
  enable:
    - deadcode
    - goconst
    - gofmt # redundant, since gofmt *should* be a no-op after gofumpt
    - gofumpt
    - goimports
    - gosimple
    - ineffassign
    - misspell
    - prealloc
    - scopelint
    - staticcheck
    - stylecheck
    - structcheck
    - unconvert
    - unparam
    - unused
    - varcheck
    - vet

